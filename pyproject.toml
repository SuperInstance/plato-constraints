[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-constraints"
version = "0.1.0"
description = "Constraint assertion engine — forbidden patterns, validation, checking"
license = {text = "MIT"}
requires-python = ">=3.8"
