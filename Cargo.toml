[package]
categories = ["science"]
keywords = ["plato", "ai", "tiles", "rooms"]
repository = "https://github.com/SuperInstance/plato-constraints"
name = "plato-constraints"
version = "0.1.0"
edition = "2021"
description = "Constraint assertion engine — forbidden patterns, validation, checking"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
